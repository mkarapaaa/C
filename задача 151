#include <stdio.h>
#include <conio.h>
#define HB 5 // размер массива
void main() 
{
int a[HB];
int obr;
int n;
int i;
printf("Введите массив (%i ",НВ); 
printf("целых чисел в одной строке)\n"); 
printf("->");
for (i=0;i<HB;i++)
  scanf("%i",&a[i]);
printf("Введите образец для сравнения ->");
scanf("%i", &obr);
n = 0;
for (i « 0 ; i < HB; i++)
  if (a[i] == obr) n++;
if ( п )
  printf("Число %i встречается в массиве %i раз", оЬr,n);
else printf("Ни один элемент массива не равен образцу");
printf("\nДля завершения работы нажмите <Enter>");
getch(); 
}
