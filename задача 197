#include "stdio.h"
#include "conio.h"
// выводит строку из звездочек 
void starline{int len)
{
  for (inti=0;i<len;i++)
  putch('*';
}
void main() (
starline(10);
puts("\nДля завершения работы нажмите <Enter>"); 
getch();
}
