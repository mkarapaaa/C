#include <stdio.h>
#include <conio.h>
void mainO
{
int m; 
int n; 
int р; 
m = 7;
printf("\nТаблица умножения на %i\n",m);
for (n = 1; n<=9; n++)
{
  p = m * n;
  printf("%ix%i=%i\n", m, n,p); 
}
printf("\nДля завершения нажмите <Enter>");
getch(); 
}
